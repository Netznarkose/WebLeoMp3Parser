<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "WebLeoMp3Parser" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>



    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" <%= link_to 'WebLeoMp3Parser', entries_path %></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <% if current_user %>
              <li class="nav-item active">
                <a class="nav-link" <%= current_page?(entries_path) ? (link_to "New Entry", new_entry_path) : (link_to "All Entries", entries_path) %><span class="sr-only">(current)</span></a>
              </li>
            <% end %>
            <% if current_user %>
              <li class="nav-item">
                <a class="nav-link" <%= link_to 'Log Out', log_out_path %></a>
              </li>
            <% else %>
              <li class="nav-item">
                <a class="nav-link" <%= link_to 'Sign Up', sign_up_path %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link" <%= link_to 'Log in', log_in_path %></a>
              </li>
            <% end %>
        </div>
      </div>
    </nav>

    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <% flash.each do |_key, value| %>
            <div class="alert alert-info" role="alert">
              <%= value %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Netznarkose <%= app_date %></p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
